name: Keep Server Awake

on:
  schedule:
    - cron: '*/10 * * * *'  # Every 10 minutes

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Send GET request to server
        run: |
          curl --max-time 120 "${{ secrets.SERVER_URL }}"
