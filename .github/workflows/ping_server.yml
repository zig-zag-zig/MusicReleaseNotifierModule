name: Keep Server Awake

on:
  schedule:
    - cron: '*/14 * * * *'  # Every 14 minutes

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Send GET request to server
        run: curl "${{ secrets.SERVER_URL }}"
